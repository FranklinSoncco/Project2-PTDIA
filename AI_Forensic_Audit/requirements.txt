streamlit>=1.37
pillow>=10.0
numpy>=1.26
requests>=2.31
google-generativeai>=0.8

# Métricas locales (MAD/FID/LPIPS/ArcFace) — utils/metrics.py
# ⚠️ pesado para el plan gratuito de Streamlit Community Cloud (1GB RAM,
# sin GPU, build con límite de tiempo). Ver advertencia en utils/metrics.py
# y en el README antes de desplegar.
torch>=2.2
torchvision>=0.17
scipy>=1.11
lpips>=0.1.4
insightface>=0.7
onnxruntime>=1.16
# insightface exige opencv-python (no headless) como dependencia propia
# -- no se puede evitar desde requirements.txt. Eso causaba
# "ImportError: libGL.so.1: cannot open shared object file" en Streamlit
# Cloud (no trae esa librería de sistema por defecto). Arreglo real:
# packages.txt con "libgl1" EN LA RAÍZ DEL REPO (no aquí adentro) -- ver
# README sección 5.1.
