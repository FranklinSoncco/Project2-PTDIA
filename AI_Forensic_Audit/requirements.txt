streamlit>=1.37
pillow>=10.0
numpy>=1.26
requests>=2.31
google-generativeai>=0.8

# Métricas locales (MAD/FID/LPIPS/ArcFace) — utils/metrics.py
# ⚠️ pesado para el plan gratuito de Streamlit Community Cloud (1GB RAM,
# sin GPU, build con límite de tiempo). Ver advertencia en utils/metrics.py
# y en el README antes de desplegar.
torch>=2.2
torchvision>=0.17
scipy>=1.11
lpips>=0.1.4
insightface>=0.7
onnxruntime>=1.16
# NO fijar opencv-python-headless aparte: insightface ya exige
# opencv-python como dependencia propia. Tener ambos instalados a la vez
# corrompe el paquete cv2 (archivos de uno pisan al otro) -- esto fue
# justo lo que rompió el despliegue (ImportError al importar utils/).
